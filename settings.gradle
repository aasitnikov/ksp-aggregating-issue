rootProject.name = "Ksp incremental"
include ':app'

include(":experiments-annotation", "experiments/annotation")
include(":experiments-processor", "experiments/processor")

void include(String name, String path) {
    include(name)
    project(name).projectDir = new File(rootDir, path)
}